import styled from 'styled-components';

const StyledFooter = styled.footer`
  text-align: center;
`;

const Footer = () => {
  return (
    <StyledFooter>
      <div>made by arvin using Next and Sanity</div>
    </StyledFooter>
  );
};

export default Footer;
